<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-gray-900 via-zinc-800 to-black text-white font-sans min-h-screen flex flex-col items-center justify-center">
    <div class="main w-full flex flex-col items-center p-10">
      <!-- Navigation -->
      <a 
        class="text-blue-500 hover:text-blue-400 font-semibold mb-8 transition-colors duration-300 text-lg transform hover:scale-105" 
        href="/"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 inline-block mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"></path>
        </svg>
        Go Back To Home
      </a>

      <!-- Task Details -->
      <div class="w-full max-w-4xl bg-zinc-800 p-8 rounded-3xl shadow-2xl transform transition-all hover:scale-105 hover:shadow-xl">
        <h1 
          class="text-5xl font-extrabold tracking-tight mb-4 bg-gradient-to-r from-teal-400 to-cyan-500 text-transparent bg-clip-text"
        >
          <%= filename %>
        </h1>
        <p class="text-xl font-light text-gray-300 leading-relaxed whitespace-pre-line">
          <%= filedata %>
        </p>
      </div>

      <!-- Extra visual effect -->
      <div class="w-32 h-32 bg-gradient-to-r from-teal-400 to-cyan-500 rounded-full absolute bottom-20 right-10 animate-pulse"></div>
    </div>
</body>
</html>
