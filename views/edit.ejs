<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-gray-900 via-zinc-800 to-black text-white font-sans min-h-screen">
    <div class="main w-full flex flex-col items-center p-10">
      <!-- Navigation -->
      <a 
        class="text-blue-500 hover:text-blue-400 font-medium mb-8 transition-colors underline" 
        href="/"
      >
        Go Back To Home
      </a>

      <!-- Form Section -->
      <form 
        action="/edit" 
        method="post" 
        class="bg-zinc-800 p-8 rounded-lg shadow-lg w-full max-w-lg"
      >
        <h1 class="text-2xl font-bold mb-6 text-center tracking-wide">Edit Task</h1>
        
        <!-- Previous Name Input -->
        <label for="previous" class="block text-gray-400 mb-2">Previous Name</label>
        <input 
          type="text" 
          id="previous" 
          name="previous" 
          value="<%= filename %>" 
          class="block w-full px-5 py-3 text-gray-300 bg-zinc-700 rounded-md shadow-inner focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 placeholder-gray-500 mb-4"
          placeholder="Enter previous name"
        >

        <!-- New Name Input -->
        <label for="new" class="block text-gray-400 mb-2">New Name</label>
        <input 
          type="text" 
          id="new" 
          name="new" 
          class="block w-full px-5 py-3 bg-zinc-700 text-gray-300 rounded-md shadow-inner focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 placeholder-gray-500 mb-6"
          placeholder="Enter new name"
        >

        <!-- Submit Button -->
        <button 
          type="submit" 
          class="w-full bg-gradient-to-r from-yellow-500 to-yellow-600 hover:from-yellow-600 hover:to-yellow-500 text-white font-semibold px-6 py-3 rounded-md shadow-md hover:shadow-lg transition-transform transform hover:scale-105"
        >
          Update
        </button>
      </form>
    </div>
</body>
</html>
