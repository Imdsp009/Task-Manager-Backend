<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notes</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-gray-900 via-zinc-800 to-black text-white font-sans">
    <div class="main w-full min-h-screen flex flex-col items-center">
      <!-- Header Section -->
      <header class="w-full py-6 bg-gradient-to-r from-blue-700 to-purple-700 shadow-lg">
        <h1 class="text-center text-4xl font-bold text-white tracking-wide animate-pulse">Task Manager</h1>
      </header>

      <!-- Form Section -->
      <div class="form p-10 w-full max-w-3xl">
        <form action="/create" method="post" class="bg-zinc-800 p-8 rounded-lg shadow-lg">
          <h2 class="text-2xl mb-4 font-semibold tracking-wide text-gray-200">Create a New Task</h2>
          <input 
            type="text" 
            class="block w-full px-5 py-3 bg-zinc-700 text-white rounded-md shadow-inner focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 mb-4 placeholder-gray-400" 
            placeholder="Title goes here" 
            name="title"
          >
          <textarea 
            class="block w-full px-5 py-3 resize-none bg-zinc-700 text-white rounded-md shadow-inner focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 mb-4 placeholder-gray-400" 
            placeholder="Write task description" 
            name="details">
          </textarea>
          <button 
            type="submit" 
            class="mt-2 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-purple-600 hover:to-blue-600 text-white px-6 py-3 rounded-md shadow-md hover:shadow-lg transition-transform transform hover:scale-105"
          >
            Create Task
          </button>
        </form>
      </div>

      <!-- Task List Section -->
      <div class="tasks p-10 w-full max-w-5xl flex flex-wrap gap-6 justify-center">
        <% if (files.length > 0) { %>
          <% files.forEach(function(val) { %>
              <div class="task w-72 p-6 rounded-lg bg-zinc-800 shadow-lg hover:shadow-xl transition-shadow">
                  <h1 class="text-xl font-bold tracking-wide text-white truncate mb-3"><%= val %></h1>
                  <div class="flex justify-between items-center">
                    <a 
                      class="text-blue-400 hover:text-blue-500 font-medium transition-colors" 
                      href="/files/<%= val %>"
                    >
                      Read More
                    </a>
                    <a 
                      class="text-gray-400 hover:text-gray-300 font-medium transition-colors" 
                      href="/edit/<%= val %>"
                    >
                      Edit
                    </a>
                  </div>
              </div>
          <% }); %>
        <% } else { %>
          <h3 class="text-gray-500 text-lg">No tasks yet</h3>
        <% } %>
      </div>
    </div>
</body>
</html>
